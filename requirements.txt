# AI Safety Surveillance System
# Install: pip install -r requirements.txt
#
# Note: Install PyTorch with CUDA separately:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Core detection
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# GPU inference
torch>=2.0.0
torchvision>=0.15.0

# Camera control
onvif-zeep>=0.2.12

# BLE laser
bleak>=0.21.0

# Web dashboard
flask>=3.0.0

# Audio alerts
pyttsx3>=2.90

# ESP32 communication — stdlib (socket, threading, queue, asyncio)
# Database — stdlib (sqlite3)
